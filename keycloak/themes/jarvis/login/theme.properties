parent=keycloak.v2
import=common/keycloak

styles=css/jarvis.css

# Jarvis's frontend is light-only (no dark mode toggle) — match it here
# rather than also handling PatternFly's dark-mode variable set.
darkMode=false
